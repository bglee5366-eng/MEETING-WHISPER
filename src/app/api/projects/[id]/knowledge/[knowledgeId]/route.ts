import { NextResponse } from "next/server";
import { createServerSupabaseClient } from "@/lib/supabase/server";
export const runtime = "nodejs";
type Context = { params: Promise<{ id: string; knowledgeId: string }> };
const unavailable = () => NextResponse.json({ error: { code: "supabase_not_configured", message: "Supabase 환경변수가 설정되지 않았습니다." } }, { status: 503 });
export async function PATCH(request: Request, context: Context) { const supabase = createServerSupabaseClient(); if (!supabase) return unavailable(); const { id, knowledgeId } = await context.params; const body = await request.json().catch(() => ({})); const patch: Record<string, string> = {}; if (typeof body.title === "string") patch.title = body.title.trim().slice(0, 200); if (typeof body.content === "string") patch.content = body.content.trim().slice(0, 20000); if (!Object.keys(patch).length) return NextResponse.json({ error: { code: "invalid_request", message: "변경할 내용이 없습니다." } }, { status: 400 }); const { data, error } = await supabase.from("project_knowledge").update(patch).eq("id", knowledgeId).eq("project_id", id).select("*").single(); if (error) return NextResponse.json({ error: { code: "supabase_error", message: "프로젝트 지식을 수정하지 못했습니다." } }, { status: 502 }); return NextResponse.json({ knowledge: data }); }
export async function DELETE(_request: Request, context: Context) { const supabase = createServerSupabaseClient(); if (!supabase) return unavailable(); const { id, knowledgeId } = await context.params; const { error } = await supabase.from("project_knowledge").update({ archived: true }).eq("id", knowledgeId).eq("project_id", id); if (error) return NextResponse.json({ error: { code: "supabase_error", message: "프로젝트 지식을 삭제하지 못했습니다." } }, { status: 502 }); return NextResponse.json({ ok: true }); }
